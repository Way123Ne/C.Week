			day03
				2017/1/2
Mon:视频学习
PART:C
*C_选择1_if概述
1>选择：定义
2〉分类：
	if
		a) if最简单的用法
		   格式：	
			if (表达式)
				语句
		   功能：
			如果表达式为真，执行语句
			如果表达式为假，语句不执行
		b) if的范围问题
		c) if...else...的用法
		d) if..else if...else...的用法
		e) C语言对真假的处理
		f) if举例--求分数的等级
		g) if的常见问题jiexi
	
	switch
*C_选择2_if最简单的用法
